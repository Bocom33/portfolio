{% extends 'base.html.twig' %}

{% block body %}

    <div class="container">
        <h5 class="p-5 text-center">Ajouter une compétence : </h5>
        <div class="d-flex">
            <div class="col-6 card-profile m-3">
                <div class="p-3">
                    {{ form_start(form) }}
                    {{ form_widget(form.title) }}
                        <button class="btn btn-success mt-3 float-right">Ajouter</button>
                    {{ form_end(form) }}
                </div>
            </div>
        </div>

        <div class="d-flex justify-content-center">
            <a class="btn btn-primary mt-5" href="">Retour à la page principale</a>
        </div>
    </div>


{% endblock %}
